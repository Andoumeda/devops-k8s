apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: devops-lab
data:
  DB_HOST: db-service
  DB_NAME: tododb
  APP_NAME: todo-backend
  APP_VERSION: "1.0.0"
  APP_ENV: kubernetes
